module.exports = {
  /* REGISTER CONTROLLER */
  register: (req, res) => {},

  /* LOGIN CONTROLLER */
  login: (req, res) => {},

  /* GET USER AUTHENTICATED */
  getUserAuthenticated: (req, res) => {},
};
