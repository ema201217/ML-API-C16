const controller = {

  // API -> GET IMAGE IN VIEW
  image: (req, res) => {},

  // API -> ALL PRODUCTS + QUERIES
  all: (req, res) => {},

  // API -> DETAIL PRODUCT
  detail: (req, res) => {},

  // API -> STORAGE PRODUCT
  store: (req, res) => {},

  // API -> UPDATE PRODUCT
  update: (req, res) => {},

  // API -> DELETE PRODUCT
  destroy: (req, res) => {},
};

module.exports = controller;
